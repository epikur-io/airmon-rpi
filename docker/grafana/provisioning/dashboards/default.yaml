apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: true
    editable: true  # Allows manual adjustments in the UI
    updateIntervalSeconds: 10
    options:
      path: /usr/share/grafana/conf/provisioning/dashboards